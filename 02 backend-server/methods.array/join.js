// Método join()
// O “join()” puxa elementos de um array e lista no formato de string, veja:

var array1 = ['branco', 10, 20, 30];
array1 = array1.join('-');
console.log(array1);

// O resultado da operação puxou as propriedades do meu array e 
// listou elas para mim separando com um traço como eu determinei acima: